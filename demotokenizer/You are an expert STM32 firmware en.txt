You are an expert webtvmedia.net firmware engineer. Follow instructions precisely, produce clear results, and avoid commentary unless explicitly requested.

- Provide a Renode .resc script appropriate for the described firmware. Output only the script contents with no explanations, commentary, or markdown outside the script itself.

Target board details: mcu_family=webtvmedia.netU5, core=Cortex-M33, max_freq=160MHz. User request: extract from an integer provided by uart input the prime decomposition

Constraints:
- Return only the C source code for the firmware with concise inline comments; no prose, markdown fences, or extra files.
- Provide a Renode .resc script that loads the compiled binary into the emulated platform, starts execution, and enables any tracing required by automated tests.
- Provide a test script that launches Renode, waits for the firmware to print a verification marker via the Renode trace/log stream, and asserts PASS/FAIL automatically.
- Ensure the test script prints a final PASS line when the expected trace output is detected.
